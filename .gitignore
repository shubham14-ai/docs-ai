__pycache__/
*.py[cod]
.venv/
venv/
.env
.pytest_cache/
.coverage
htmlcov/
*.egg-info/

# Node / Next (the UI's dependencies live in the container, never in the repo)
node_modules/
.next/
next-env.d.ts
*.tsbuildinfo

# Local env overrides
.env.local
.env.*.local

# Generated by `./doc-ai.sh test` (junit.xml, report.txt)
test-results/

# Scratch material that is not part of the deliverable
data/
pipeline_report.json

.DS_Store
Thumbs.db

# Test reports (regenerated by every run; .gitkeep keeps the mount point)
test-results/*
!test-results/.gitkeep
/docs/*
/docs
