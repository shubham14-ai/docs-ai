[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
addopts =
    -ra
    --strict-markers
    --tb=short
markers =
    integration: exercises the real MongoDB and Redis (skipped when unreachable)
    unit: pure-Python, no external dependencies
    edge: edge-case or boundary condition test
